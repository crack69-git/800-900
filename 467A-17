#include<bits/stdc++.h>
using namespace std;
int main(){
    int n,cnt=0;
    cin>>n;
    int p[n],q[n];
    for(int i=0;i<n;i++){
        cin>>p[i];
        cin>>q[i];
        int c=q[i]-p[i];
        if(c>=2){
            cnt++;
        }
    }
    cout<<cnt;
return 0;
}
